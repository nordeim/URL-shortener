# Docker Environment Variables Template
# Copy this file to .env.docker and fill in your actual values

# ===========================================
# Next.js Application Configuration
# ===========================================
NEXT_PUBLIC_SUPABASE_URL=http://localhost:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# ===========================================
# Supabase PostgreSQL Configuration
# ===========================================
# Database settings
POSTGRES_DB=postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# ===========================================
# Optional: Development/Production Settings
# ===========================================
# Set to 'development' for verbose logging, 'production' for minimal logging
NODE_ENV=production

# Port configuration (if you want to change defaults)
# APP_PORT=3000
# DB_PORT=5432

# ===========================================
# Security Notes
# ===========================================
# 1. In production, use strong passwords and secure connections
# 2. Never commit actual credentials to version control
# 3. Use proper environment variable management in production
# 4. Enable SSL/TLS for database connections in production
# 5. Rotate keys regularly and use vault/secret management systems